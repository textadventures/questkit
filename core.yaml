command: lookat
patterns:
    template: lookat
action:
    script: |
        var text = get(object, "look");
        if (text) {
            msg(text);
        }
        var script = getscript(object, "look");
        if (script) {
            script();
        }